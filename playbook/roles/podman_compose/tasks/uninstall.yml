---
- name: Uninstall Podman Compose
  block:
    - name: Remove file
      ansible.builtin.file:
        path: "{{ user_home }}/{{ item }}"
        state: absent
        force: true
      with_items:
        - "{{ dest_path }}"
